CREATE TABLE IF NOT EXISTS geolocation(location_id INT GENERATED ALWAYS AS IDENTITY, user_id uuid NOT NULL, latitude float(32) NOT NULL, longitude float(32) NOT NULL, accuracy varChar(255), timestamp TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (location_id), CONSTRAINT fk_user FOREIGN KEY (user_id) REFERENCES users(user_id));